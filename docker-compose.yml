services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: maison-valery:latest
    container_name: maison-valery
    ports:
      - "${HTTP_PORT:-8080}:80"
    restart: unless-stopped
